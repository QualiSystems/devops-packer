---
driver:
    name: hyperv
    vm_generation: 2
    disable_secureboot: true
    enable_guest_services: true
    memory_startup_bytes: 2147483648
    dynamic_memory: true
    vm_switch: "'Hyper-V'"

provisioner:
  name: chef_zero
  roles_path: '../roles'

transport:
  name: ssh
  elevated: true
  password: 'vagrant'
  username: 'vagrant'

platforms:
  - name: ubuntu-17.04
    os_type: unix
    driver:
        parent_vhd_folder: '../../.output/hyperv/ubuntu1704/Virtual Hard Disks'
        parent_vhd_name: 'ubuntu1704.vhdx'

verifier:
      name: inspec

suites:
  - name: common
    run_list:
#      - recipe[packer-ubuntu::install_hyperv_integration_services]
#      - recipe[packer-ubuntu::configure_ssh]
#      - recipe[packer-ubuntu::add_vagrant_ssh_key]
#      - recipe[packer-ubuntu::set_time_zone]
#      - recipe[packer-ubuntu::purge_apt_packages]
#      - recipe[packer-ubuntu::delete_redundant_files]       
#      - recipe[packer-ubuntu::configure_xrdp_to_use_xfce]
#      - role[monodevelop_6.1.2.44]
#      - recipe[packer-ubuntu::install_xrdp]
#      - recipe[packer-ubuntu::configure_apt_to_filter_packages_files]
#      - recipe[packer-ubuntu::clean_apt]
#      - recipe[packer-ubuntu::install_mono]
#      - recipe[packer-ubuntu::build_monodevelop_6.1.2.44]
      - recipe[packer-ubuntu::create_monodevelop_desktop_shortcut]

    attributes:
        monodevelop_verison: "6.1.2.44"
        box_settings:
            is_desktop: "false"
            time_zone: 'Asia/Jerusalem'
            ssh_user: 'vagrant'
            ssh_user_password: 'vagrant'
            ssh_user_group: 'vagrant'